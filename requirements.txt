# Versions are pinned deliberately. Left unpinned, each deployment
# installs whatever is newest, and a combination that worked last week
# stops working this one — which is what happened with pandas 3 and
# pyarrow 25.

pdfplumber==0.11.4
python-docx==1.1.2
sentence-transformers==3.3.1
psycopg[binary]==3.2.3
google-genai==1.2.0
streamlit==1.41.1
python-dotenv==1.0.1

# Google Drive
google-api-python-client==2.155.0
google-auth==2.37.0
google-auth-httplib2==0.2.0

# pinned because Streamlit's dataframe needs these two to agree
pandas==2.2.3
pyarrow==18.1.0
numpy==1.26.4
